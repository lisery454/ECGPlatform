<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:ECGPlatform"
                    xmlns:i="http://schemas.microsoft.com/xaml/behaviors">
	<Style TargetType="{x:Type Page}"
	       x:Key="BasePageStyle">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type Page}">
					<Grid>
						<Border Background="{DynamicResource BrushPrimary10}"
						        CornerRadius="10">
							<i:Interaction.Behaviors>
								<local:ShadowBehavior Background="{DynamicResource BrushWhite}" />
							</i:Interaction.Behaviors>
							<ContentPresenter Content="{Binding Path=Content, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Page}}}" />
						</Border>
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
</ResourceDictionary>